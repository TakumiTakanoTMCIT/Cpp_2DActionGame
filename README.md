# 横スクロール2Dアクションゲーム

SDL2 と C++ で制作している 2D アクションゲームです。
プレイヤーの移動、ジャンプ、敵との接触判定、踏みつけ、マグマ、ステージ遷移、タイトル画面、クリア画面、効果音までをひと通り実装しています。

「まずはゲームサイクルを最後まで成立させる」を目標に作っていて、現在は

- タイトル画面
- プレイ
- 死亡してリトライ
- ゴールして次ステージへ
- 最終ステージクリア

まで一連の流れが遊べる状態です。

## Features

- 左右移動とジャンプ
- ブロックとの当たり判定
- 敵との接触で死亡
- 敵を上から踏んで撃破
- マグマによる即死判定
- 敵の巡回移動
- ステージ文字列からマップを生成
- 複数ステージ対応
- タイトル画面 / クリア画面
- UI 決定音の再生

## Controls

- `Left Arrow`: 左移動
- `Right Arrow`: 右移動
- `Space`: ジャンプ / タイトル開始 / クリア画面終了
- `Enter`: タイトル開始 / クリア画面終了

## 使用した物

- C++
- SDL2
- SDL2_image
- SDL2_ttf
- SDL2_mixer

## ファイル構成

```text
.
├── main.cpp
├── App.h / App.cpp
├── Player.h / Player.cpp
├── Stage.h / Stage.cpp
├── SoundManager.h / SoundManager.cpp
└── assets/
```

### ファイルの担当と説明

- `main.cpp`
  ゲーム全体の起動とメインループだけを担当します。

- `App.h / App.cpp`
  SDL の初期化、入力処理、更新、描画、終了処理など、アプリ全体の進行を管理します。

- `Player.h / Player.cpp`
  プレイヤーの移動、ジャンプ、重力、当たり判定、死亡判定、ゴール判定を担当します。

- `Stage.h / Stage.cpp`
  ステージデータ、ブロック、敵、マグマ、ゴール、ステージ生成を担当します。

- `SoundManager.h / SoundManager.cpp`
  UI 効果音の初期化、再生、終了処理を担当します。

## ビルド方法

Homebrew で必要なライブラリを導入したあと、以下のコマンドでビルドできます。

```bash
g++ main.cpp App.cpp SoundManager.cpp Stage.cpp Player.cpp -std=c++17 -I/opt/homebrew/include -L/opt/homebrew/lib -lSDL2 -lSDL2_image -lSDL2_ttf -lSDL2_mixer -o main
```

## 実行方法

```bash
./main
```

## 必要なもの

以下のライブラリが必要です。

- `SDL2`
- `SDL2_image`
- `SDL2_ttf`
- `SDL2_mixer`

フォント、画像、効果音などのアセットは、`assets/` ディレクトリに配置しています。

## ステージ

ステージは文字列配列から生成しています。
タイル文字ごとの意味は以下の通りです。

- `P`: プレイヤー開始位置
- `G`: 地面
- `B`: ブロック
- `E`: 敵
- `M`: マグマ
- `C`: ゴール
- `.`: 空白

この方式にしているので、マップの追加や調整をコード上で比較的簡単に行えます。

## 現状の進捗度合と展望

現在のプロジェクトは、試作段階を越えて「ゲームとして一周遊べる」状態です。
今後は以下のような拡張を考えています。

- アニメーション追加
- ステージ数の増加
- UI 表示の強化
- 効果音や BGM の拡充
- ファイル構成のさらなる整理

## 開発で意識していたこと

このプロジェクトでは、機能追加だけでなく、

- main関数の肥大化を防ぐ
- 責務ごとにファイルを分ける
- 小さくても読みやすい構成にする

といった、コード整理も意識しながら開発を進めています。

そのため、単に「動けばいい」ではなく、今後の拡張や保守もしやすい形を目指しています。


## お借りした素材一覧
- フォント:ベストテン
- 効果音:効果音ラボ
- ドット絵は自作しました

## 作者

Takumi Takano
2026/4/10
